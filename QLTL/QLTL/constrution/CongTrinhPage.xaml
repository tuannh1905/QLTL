<Page x:Class="QLTL.views.CongTrinhPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Danh sách công trình" 
      Loaded="Page_Loaded">

    <Grid Margin="20" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="lblTitle" 
                   Text="DANH SÁCH CÔNG TRÌNH THỦY LỢI" 
                   FontSize="24" FontWeight="Bold" Foreground="#2C3E50"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <DataGrid x:Name="dtgCongTrinh" Grid.Row="1"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeight="30" FontSize="14">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CongTrinhID}" Width="50"/>
                <DataGridTextColumn Header="Loại CT" Binding="{Binding LoaiCongTrinh}" Width="150" FontWeight="Bold"/>
                <DataGridTextColumn Header="Địa điểm" Binding="{Binding DiaDiem}" Width="250"/>
                <DataGridTextColumn Header="Mô tả chi tiết" Binding="{Binding MoTa}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>